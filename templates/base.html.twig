<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Hello !{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
            <link rel="preconnect" href="https://fonts.gstatic.com">
            <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@100;400;900&display=swap" rel="stylesheet">
            <link rel="stylesheet" href="{{ asset('css/base/style.css') }}">
        {% block stylesheets %}{% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>

        <header>
            <div class="container">
                <nav>
                    <ul>
                        <li>
                            <a href="{{ path('index') }}" title="Accueil">Accueil</a>
                            <a href="{{ path('coiffeurs') }}">Formulaires Dynamiques</a>
                        </li>
                    </ul>
                </nav>
            </div>
        </header>

        {% block body %}


        {% endblock %}
    </body>
</html>
