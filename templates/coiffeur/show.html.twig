{% extends 'base.html.twig' %}

{% block title %}Coiffeur{% endblock %}

{% block body %}
    <h1>Coiffeur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coiffeur.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ coiffeur.titre }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ coiffeur.texte }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ coiffeur.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('coiffeur_index') }}">back to list</a>

    <a href="{{ path('coiffeur_edit', {'id': coiffeur.id}) }}">edit</a>

    {{ include('coiffeur/_delete_form.html.twig') }}
{% endblock %}
